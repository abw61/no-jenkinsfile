pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'this is to build the app'
      }
    }
    stage('Test') {
      steps {
        echo 'this is to test'
      }
    }
    stage('deploy') {
      steps {
        echo 'his is to deploy'
      }
    }
  }
}